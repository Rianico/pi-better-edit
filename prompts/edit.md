Edit a range of lines in a text file with the exact payload `{ "edit": [path, [remove_from, remove_to], replacement_text] }`. `path` is a non-empty file path or `null` to infer it from the anchors. The range is exactly two inclusive bare 3-character HASH anchors from read output (for example `"aB3"`), never line content. `replacement_text` is the complete replacement; use `""` to delete the range.
